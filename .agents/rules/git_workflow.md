# Git & Publishing Workflow Rules

- **DO NOT automatically commit or push (`git push`) to GitHub without explicit user approval.**
- Always implement local code changes first.
- Ask the user to preview changes locally (e.g. at `http://localhost:9002`) and obtain their confirmation before pushing any changes to GitHub.
